<h2><%= l(:label_platform_new) %></h2>

<% labelled_tabular_form_for :platform, @platform, :url => {:action => 'new'} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_commit) %>
<% end %>
